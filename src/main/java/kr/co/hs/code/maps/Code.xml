<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="Code">
    <!-- 직원 리스트 가져오기. -->
    <select id="selectCodeList" parameterType="kr.co.hs.code.dto.CodeDTO" resultType="kr.co.hs.code.dto.CodeDTO">
        <![CDATA[
            SELECT  C_CODE_VALUE
            ,       GET_CODE_NAME(C_CODE_ID, #{langCd}) as C_CODE_NAME
            FROM C_CODE
            WHERE C_CODE = #{cCode}
            AND USE_YN = 'Y'
            ORDER BY SORT ASC
        ]]>
    </select>
</mapper>