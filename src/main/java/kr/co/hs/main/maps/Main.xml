<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="main">
    <select id="selectNote" parameterType="int" resultType="kr.co.hs.main.dto.NoteDTO">
        <![CDATA[
        SELECT * FROM tb_note WHERE emp_cd=#{empCd} ORDER BY id DESC LIMIT 5
        ]]>
    </select>

    <delete id="xoaNote" parameterType="int" >
        DELETE FROM tb_note WHERE id=#{id}
    </delete>

    <insert id="addNote" parameterType="kr.co.hs.main.dto.NoteDTO">
        INSERT INTO tb_note(emp_cd,contents) VALUES(#{empCd},#{contents})
    </insert>

</mapper>